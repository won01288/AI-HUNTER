# 로드맵

## Phase 1 — 가입 + 온보딩 (지금 여기)
- [ ] Next.js 프로젝트 초기 세팅 (Tailwind CSS 포함)
- [ ] `.env.example` / `.gitignore` 설정 (시크릿 관리 규칙, CLAUDE.md 참고)
- [ ] Turso DB 연결 (src/lib/db.ts)
- [ ] users, user_preferences, priority_weights 테이블 생성 (kakao_id/nullable email 포함)
- [ ] 이메일/비밀번호 회원가입·로그인 (비밀번호 bcrypt 해시, 동의 체크 포함)
- [ ] 카카오 개발자 계정 본인인증 → 앱 등록 → 카카오 로그인 연동
      (기본 동의항목만으로 우선 연동, 이메일 없이도 가입되게 처리)
- [ ] 카카오 "비즈 앱 전환" 신청 (이메일 동의항목용, 승인까지 별도 소요기간
      있을 수 있음 — 승인 전까지는 email이 비어있는 카카오 사용자로 운영)
- [ ] 워크넷 공통코드 API로 지역/직종 코드 목록 미리 확보 (온보딩 select 옵션용)
- [ ] 1단계 온보딩 폼 UI (필수 5문항, 재방문 이어가기 포함)
- [ ] 2단계 온보딩 폼 UI (선택 정교화)
- [ ] Render 배포 (스테이징)

## Phase 2 — 데이터 연동 및 매칭 (docs/roadmap_phase2.md 참고)
- [x] 워크넷 웹사이트 크롤러 개발 (src/lib/collectors/worknet.ts, 목록 페이지
      GET+파싱 방식. 상세 페이지 요청은 불필요한 것으로 확인되어 생략함)
- [x] job_postings 테이블 설계 + 수동 배치 스크립트 (`npm run collect:worknet`).
      **Render Cron Job 등록은 아직 안 됨** — 배포 설정이라 수동으로 등록 필요
      (스크립트를 커맨드로 등록하면 됨, 하루 1회 권장)
- [x] 매칭 스코어링 로직 (priority_weights 반영, 미입력 시 균등 배분 기본값 적용)
- [x] 사용자용 매칭 결과 화면 (상위 10개 카드, design-guide.md 스타일)
- [x] 매칭 결과 확인용 내부 대시보드 (개발자용, breakdown 표시, 로컬 전용)

## Phase 3 — 정기 추천 발송
- [ ] 이메일 발송 연동
- [ ] 카카오 알림톡 연동 (비즈메시지, 발송 동의 UI 포함)
- [ ] 발송 스케줄링 (주기 설정)
- [ ] 추천 결과 피드백 수집 (관심있음/없음) → 가중치 보정 루프

## Phase 4 — 유료 확장 (베타 이후, 별도 기획 필요)
- [ ] 직업정보제공사업 신고 여부 재검토
- [ ] 개인정보 보호 조치 정식 구현 (암호화, 파기 정책, 동의 관리)
- [ ] 자기소개서 작성 지원 (AI 첨삭)
- [ ] AI 직무 포트폴리오 빌더
- [ ] 결제/구독 시스템
